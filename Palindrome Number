#Palindrome number can be solved by simple slicing
#121 is palindrome but 10, -121 is not

class Solution(object):
  def Palindrome(self, x):
    s = str(x)

    if s == s[::-1]:
      return True
    else:
      return False
